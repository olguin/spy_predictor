# Repaired successor configuration and evaluator integration

Current status: the successor was subsequently registered and stopped for compute
budget on 2026-09-09. See [registered profile/stop](CYCLE1_SUCCESSOR_PROFILE.md).
The following records the preceding deterministic integration milestone.

2026-09-09. Increment B remains synthetic-only. The immediate configuration and
integration work is implemented. No random development draw, registration, locked
validation, real-data rebuild or historical evaluation occurred in this increment.

`config/cycle1-simulation-v2.json` and its exact schema select the repaired equation
identity, retain all ten scenarios and their acceptance roles, and bind the existing
proposal, stopped predecessor, preregistration and source audit. The loader verifies
the entire authority chain and exact numerical constraints. The final configuration
is **unregistered**; it grants no execution permission. Historical files stay intact.

Sixteen disjoint primitive stream recipes specify cadence, distribution, vector
width and SeedSequence ordinal. Monthly SPY/QQQ Students, daily normals, macro
innovations, publication missingness, origin missingness, crash occurrence/timing
and baseline-regime uniforms have separate children. Corporate actions remain
deterministic. These are sampler specifications; a registered random sampler is
still required. No random generator is opened by the configuration loader.

`cycle1_successor_pipeline.py` adapts repaired daily evidence into the existing
shared evaluator without modifying the stopped v1 modules. It preserves all monthly
origins and unavailable feature rows, uses the actual valuation/stress/direction
scores and realized daily volatility, derives overlapping annual excess labels
from shareholder daily closes and publication-admissible cash, and checks their
equality to the sum of repaired monthly returns. Daily-close drawdown labels and
next-session-open execution tapes come from the same path. Policy calculations
include dividend entitlements, splits, drifted weights, turnover, liquidation,
and the unchanged base/stressed transaction costs.

The deterministic full-calendar fixture spans January 1990–July 2026. It exercises
68 selection and 97 confirmation months, both fit modes, conditional QQQ transfer,
both challengers' costed policies, and downside forecast calculations. Forced
branch exercises are separately labeled and cannot change counted qualification.
The ordinary downside assessment correctly reports insufficient support: its
unchanged ten-distinct-episode gate is not bypassed for this fixture. Passing
arithmetic tests is not a passing statistical audit.

Reproduce with:

```bash
npm run cycle1:integration-check
npm run check
```

The integration command publishes an immutable report with code hashes, final
configuration identity and deterministic procedure results. Its output is not a
development runtime profile or power estimate. Existing repair fixtures cover
positive-price accounting and future-input invariance; new integration tests cover
feature/target mapping, uncompressed calendars, conditional branches, cost cases,
legacy/path-gap rejection, and rejection of rehashed authority mutations.

Published [deterministic integration report](../reports/cycle1-successor-integration-0f15218dc8e60926/report.json):
`0f15218dc8e60926170ab6bfa4dde676db5b750edb1c0b1e1067020efae0a286`.
Final configuration identity:
`2a4b1daf12b3665d78ca25b369320cd2c23c310103818a1c548d5cf1ec37ad6f`.
Verification: `npm run check` passed (15 TypeScript and 283 Python tests).
The fixture includes 9,191 daily sessions per instrument and 439 monthly cutoffs.
Its primary result is unqualified; forced policy exercises fail their economic
gates and downside reports one episode against ten required. These are expected
deterministic fixture outputs, not false-positive or power estimates.

Next: atomically register the single successor with final code, schema, calendar
inventory and fixture identities; implement/enforce its random sampler and draw
ledger; then profile the complete procedure under the existing 120-second profile
and 3,600-second projected validation budgets. Only a passing profile can release
the single locked audit of 2,000 replications per scenario. No reduced replication
count, effect change or acceptance-threshold relaxation is permitted. Final
real-dataset feature/target parity and qualification remain a separate gate.

IBKR Gateway is unnecessary for this synthetic work. The secondary notebooks'
verified current-data snapshot remains a separate pending task.
